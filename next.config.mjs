/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['riyora-organic.vercel.app'], // Add your image domains here
  },
};

export default nextConfig;
